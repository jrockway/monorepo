actions:
    - name: "Test"
      triggers:
          push:
              branches:
                  - "*"
      pull_request:
          branches:
              - "*"
      steps:
          - run:
                "bazel --config=remotecache test //...
                //:style_tests  --remote_grpc_log=$BUILDBUDDY_ARTIFACTS_DIRECTORY/grpc.log"
